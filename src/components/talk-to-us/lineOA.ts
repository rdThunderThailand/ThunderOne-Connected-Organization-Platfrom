// TODO: redirect to the real LINE OA deep link once the LINE ID/message is
// finalized, e.g. `https://line.me/R/oaMessage/@LINE_ID/?{message}`. Opening
// the LINE app/conversation itself is out of scope for this flow.
export function openLineOA() {
  console.log("[TalkToUs] openLineOA() called — TODO: redirect to real LINE deep link");
}
